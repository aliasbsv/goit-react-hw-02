// src/components/Notification/Notification.jsx
import css from './Notification.module.css';

const Notification = () => (
  <div>
    <p className={css.notification}>No feedback given yet.</p>
  </div>
);

export default Notification;
